# MonoBrick QA Report

Date: 2026-03-23  
Scope: local verification for current mobile/Base-ready pass

## Automated Checks

### Build/Syntax

- PASS: `node --check src/app.js`
- PASS: `node --check src/base-hooks.js`
- PASS: `node --check src/bootstrap.js`
- PASS: `node --check src/game.js`
- PASS: `node --check backend/src/index.mjs`

### Frontend Asset Freshness / Cache-Busting

- PASS: `index.html` served with `v6 Cartridge`
- PASS: `bootstrap.js` served with build id `20260323e`
- PASS: `runtime-config.json` served with `apiBaseUrl` and `walletAdapterUrl`
- PASS: `wallet-adapter.js` served

### Backend Positive Path

Validated against production backend (`backend/src/index.mjs`) on an isolated test port.

- PASS: `/health` returns healthy payload
- PASS: `/auth/nonce` returns nonce + expiry
- PASS: `/auth/verify` accepts valid SIWE-style message + signature and returns token
- PASS: nonce replay blocked (`/auth/verify` same nonce returns 401)
- PASS: `/monobrick/submit-score` accepts valid signed payload with auth token
- PASS: `/monobrick/leaderboard` includes submitted row
- PASS: `/monobrick/claim-og-badge` accepts eligible claim
- PASS: claim endpoint behaves idempotently for same wallet (existing claim returned)

### Backend Negative Path

- PASS: submit without auth returns 401
- PASS: claim without verified eligible submission returns 403
- PASS: wrong network submit payload rejected with 400

## Mobile UI/Gameplay Fix Verification Status

Implemented:

- adaptive stage word fitting for all campaign stages (1-20)
  - single-line preferred
  - tightened spacing if needed
  - split lines only as fallback
- Stage 1 `BASE` mobile rendering fallback without cramped logo collisions
- narrow-screen home stat readability improvements
- mobile board spacing/brick size tuning

Still requires manual visual confirmation in-device:

- Stage text appearance quality at stages 1, 3, 10, 20
- one-handed touch comfort and accidental-tap behavior
- result screen CTA hierarchy and safe-area behavior

## Remaining Manual QA (Base App / Real Wallet)

1. Guest-first open -> play (no wallet prompt)
2. Connect wallet flow in Base app webview
3. SIWE auth prompt and re-auth behavior after expiry
4. Submit score flow with real wallet signing
5. Reward claim flow with retry states on transient failure
6. Share flow behavior inside Base app context
7. Performance sanity on low-end mobile device

## Conclusion

Local architecture and trust-path checks are passing.  
Remaining risk to 9.5 readiness is external-environment validation (Base app runtime behavior + production infra deployment quality).

